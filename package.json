{
  "name": "radiocloud",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "poi",
    "build": "poi build",
    "favicons:generate": "real-favicon generate ./favicons.config.json ./dist/favicons.json ./dist/",
    "favicons:inject": "real-favicon inject ./dist/favicons.json ./dist/ ./dist/200.html",
    "favicons": "npm run favicons:generate && npm run favicons:inject",
    "deploy": "npm run build && npm run favicons && surge ./dist/ -d https://radiocloud.surge.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "cli-real-favicon": "^0.0.6",
    "git-rev-sync": "^1.9.1",
    "poi": "^9.5.5",
    "poi-preset-buble": "^9.0.1",
    "stylus": "^0.54.5",
    "stylus-loader": "^3.0.1"
  },
  "dependencies": {
    "choices.js": "^3.0.2",
    "fade": "^0.2.0",
    "plyr": "^2.0.18"
  }
}
